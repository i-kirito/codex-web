# Run `npm run setup` to generate a local .env, or copy this file manually.

APP_NAME=Codex Web Example

# Required. Password used to sign in to the Web UI.
CODEX_WEB_PASSWORD=<replace-with-a-strong-password>

# Local-only by default. Use 0.0.0.0 only behind a trusted LAN/firewall.
PORT=36354
HOST=127.0.0.1
PUBLIC_HOST=127.0.0.1
PORT_MIN=30000
PORT_MAX=39999

# Keep this stable so login sessions survive restarts.
SESSION_SECRET=<replace-with-at-least-32-random-bytes>
SESSION_TTL_HOURS=168
COOKIE_SECURE=false

# Optional. Enables the read-only Homepage statistics endpoint.
HOMEPAGE_API_TOKEN=<optional-random-read-only-api-token>
HOMEPAGE_MODEL_CACHE_SECONDS=60

# Optional. Enables the dedicated read-only quota monitor API. Set one of these;
# the direct token takes priority. The token file must be a regular 0600 file.
CODEX_WEB_QUOTA_MONITOR_TOKEN=
CODEX_WEB_QUOTA_MONITOR_TOKEN_FILE=/absolute/path/to/quota-monitor.token

# Optional. Shows CPA Codex, Sub2API, Grok2API and DeepSeek official quotas in the authenticated Web UI.
# Keep real keys only in the local .env file.
CPA_QUOTA_BASE_URL=http://127.0.0.1:8327
CPA_QUOTA_API_KEY=
SUB2API_BASE_URL=https://sub.example.com
SUB2API_API_KEY=
GROK2API_BASE_URL=http://127.0.0.1:8100
GROK2API_ADMIN_PASSWORD=
# Optional. DeepSeek official API quota: balance is fetched from /user/balance in real time;
# cumulative token usage / spend is accumulated from this service's actual API calls.
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=
# Optional. Adds cached Codex provider-account 5-hour/7-day usage from the Sub2API admin API.
SUB2API_ADMIN_API_KEY=
SUB_QUOTA_TIMEOUT_MS=10000
SUB_QUOTA_CACHE_SECONDS=30
# Optional. Total Codex App credit pool used only as the remaining-progress baseline.
CODEX_APP_CREDIT_LIMIT=
# Optional: cpa-codex | sub2api | multi (managed automatically by the Web settings dialog)
SUB_QUOTA_PROVIDER=multi

# Image Prompt library updates from freestylefly/awesome-gpt-image-2.
IMAGE_PROMPT_AUTO_SYNC=true
PLAYGROUND_UPDATE_ENABLED=true
IMAGE_PROMPT_SYNC_INTERVAL_MINUTES=360
IMAGE_PROMPT_SYNC_TIMEOUT_MS=20000
PLAYGROUND_PROXY_TIMEOUT_MS=690000
CODEX_WEB_SHUTDOWN_GRACE_MS=120000
# Comma-separated origins allowed in addition to configured Codex Providers.
PLAYGROUND_PROXY_ALLOWED_ORIGINS=https://api.example.com
# Optional. Raises the GitHub API rate limit; keep the real token only in .env.
IMAGE_PROMPT_GITHUB_TOKEN=

# Host Codex CLI integration.
# `npm run setup` prefers the Codex binary bundled with ChatGPT/Codex App on macOS.
CODEX_BIN=/absolute/path/to/codex
CODEX_HOME=/home/<user>/.codex
CODEX_CONFIG_WRITABLE=false
CODEX_WEB_ENV_FILE=/absolute/path/to/codex-web/.env
CODEX_WEB_RUNTIME_DIR=/absolute/path/to/codex-web/runtime
# Optional. Runtime-only TSV mapping from old working directories to new ones.
# Leave empty to disable. The mapping file itself should remain local.
CODEX_WEB_CWD_MIGRATIONS_FILE=
# Optional. Comma-separated trusted roots for absolute images referenced by
# assistant messages, for example /Users/me/portrait-gallery/data.
CODEX_WEB_LOCAL_IMAGE_ROOTS=
# Optional. Comma-separated trusted files or directory roots for absolute
# local-file links in assistant messages. Files are served as authenticated plain text only.
CODEX_WEB_LOCAL_FILE_ROOTS=

# Bidirectional Codex App session synchronization.
APP_SERVER_REQUEST_TIMEOUT_MS=30000
# Optional. Proxy used by the Codex App Server quota probe (also exported as
# HTTP(S)_PROXY/ALL_PROXY in both upper- and lower-case forms).
CODEX_APP_SERVER_PROXY=
# Prefer the currently open Codex App window for existing-thread turns.
CODEX_DESKTOP_IPC_ENABLED=true
CODEX_EXISTING_THREAD_APP_SERVER_FALLBACK=false
CODEX_DESKTOP_IPC_TIMEOUT_MS=20000
# Optional override. Leave empty to use the current user's Codex Desktop socket.
CODEX_DESKTOP_IPC_SOCKET=
# 0 keeps the complete Codex App transcript. Positive values cap resource use.
NATIVE_SESSION_MAX_READ_MB=0
NATIVE_SESSION_MAX_MESSAGES=0
NATIVE_SESSION_MAX_ITEMS=100
NATIVE_SESSION_POLL_MS=1000

# Defaults used when creating a new conversation.
DEFAULT_PROVIDER=
DEFAULT_MODEL=
DEFAULT_CWD=/absolute/path/to/<workspace>
DEFAULT_SANDBOX=workspace-write
DEFAULT_APPROVAL=on-request

# Emergency deployment policy. When true, the Web UI locks every task to
# danger-full-access with approval=never. Keep false for selectable permissions.
FORCE_FULL_ACCESS=false
